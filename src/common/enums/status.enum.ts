export enum StatusEnum {
  active = 'active',
  inactive = 'inactive'
}

export enum ApprovalStatusEnum {
  approve = 'approve',
  reject = 'reject'
}

export enum VoucherStatusEnum {
  active = 'active',
  inactive = 'inactive',
  used = 'used'
}
