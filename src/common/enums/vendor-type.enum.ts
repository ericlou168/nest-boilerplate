export enum IndieVendorTypeEnum {
  localSupplier = 'local_supplier',
  foreignSupplier = 'foreign_supplier'
}

export enum CompanyVendorTypeEnum {
  nonTaxableEntity = 'non_taxable_entity',
  taxableEntity = 'taxable_entity',
  foreignCompany = 'foreign_company'
}

export enum VendorTypeEnum {
  individual = 'individual',
  company = 'company',
}
